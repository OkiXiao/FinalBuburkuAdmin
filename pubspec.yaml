name: buburku
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2  # ✅ Tambahkan ini
  firebase_core: ^3.8.0
  cloud_firestore: ^5.4.2
  firebase_auth: ^5.3.3
  flutter_local_notifications: ^17.1.0  
  http: ^1.1.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
